{
  "name": "preact",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --base=/preact/",
    "build": "tsc && vite build --base=/preact/ --outDir=docs",
    "preview": "vite preview --port 5173 --host 0.0.0.0 --base=/preact/ --outDir=docs"
  },
  "dependencies": {
    "preact": "^10.19.6"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.2",
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vite-plugin-stylex": "^0.8.1"
  }
}
